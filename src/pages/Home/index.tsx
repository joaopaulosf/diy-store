import { Header } from "layout/Header";
import { Main } from "./components";

export const Home = () => {
  return (
    <section className="home">
      <Header />
      <Main />
    </section>
  );
};
